# Netanya Incident Service Environment Configuration
# Copy this file to .env and modify as needed
# For pre-configured environments, use env.development or prod.env

# Core Application Settings
ENVIRONMENT=development
DEBUG_MODE=true
LOG_LEVEL=DEBUG
PORT=8000

# SharePoint Integration
SHAREPOINT_ENDPOINT=http://mock-sharepoint:8080/api/incidents

# Docker-specific settings (optional)
BUILD_TARGET=development
CONTAINER_SUFFIX=-dev

